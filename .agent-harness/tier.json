{
  "tier": 2,
  "name": "daily-driver",
  "authority": {
    "push": "free",
    "merge": "free"
  },
  "flags": {
    "sensitive_data": false,
    "wave_mode": false,
    "dormant_production": false,
    "relaxed_work_loss_guards": false
  },
  "model_routing": {
    "harness_and_review": "top",
    "slices": "top",
    "maintenance": "mid"
  },
  "budgets": {
    "standing_context_tokens": 2500,
    "session_baseline_tokens": null
  },
  "human_todo": "HUMAN_TODO.md",
  "runtimes": ["claude"],
  "last_reviewed": "2026-07-27",
  "notes": "Promoted T1 -> T2 on 2026-07-27: the product is fully built (CLI + composite action.yml + 80 tests + CI with an action self-test lane) and is published for outside consumers, which is the durable-use trigger NEXT.md named. No production signals that would justify more: no deploys, no money, no user data, no secrets (the offline fake provider replays committed fixtures; the demo, tests and CI need no API key), so authority stays push free / merge free per owner directive agent-harness#91 -- the tier's gate does the verifying. Migrated from legacy .claude/tier.json, which was deleted in the same commit. sensitive_data stays false and must be re-reviewed the moment a real model-provider adapter lands (NEXT.md housekeeping trigger): keys will come from the environment only, never the repo. The deny floor is deliberately NOT vendored here -- Claude gets it from the global PreToolUse hook (~/.claude/hooks/dispatch.py) and a repo copy would double-spawn. bypassPermissions lives in gitignored .claude/settings.local.json; committed .claude/settings.json keeps acceptEdits + the deny list as the fresh-clone baseline."
}
